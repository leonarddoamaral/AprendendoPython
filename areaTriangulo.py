altura= float(input("Insira a altura do triangulo que deseja calcular: "))
base= float(input("Insira a base do triangulo que deseja calcular: "))
areaTri = float((base*altura)/2)
print("Area do triangulo é de; ",areaTri)